function displayValue(value: string | number) {
    console.log(value);
}

displayValue('holaaaa')
displayValue(666)
// displayValue(true)



function getLength(input: string | any[]): number {
    return input.length
}